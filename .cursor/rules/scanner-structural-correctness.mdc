---
description: Structural invariants that must not regress in SMT/FVG detection (timestamp matching, FVG search origin, SMT_IN_FVG confirmation-bar anchoring, dedup, wick-based SMT, equal high/low). Always applied.
globs:
alwaysApply: true
---

# Structural correctness (do not regress these)

- ES/NQ `sw1` and `sw2` swing comparisons must use **EXACT** timestamp match —
  zero tolerance, same bar only. Never loosen this to a ±N minute tolerance,
  even to raise signal count. (This is defect **S3** — see the v8.8 audit-scope
  rule for its audit ID and current fix status.)
- FVG search must start from `sw2_conf_idx`, not from the signal bar.
- SMT_IN_FVG's pre-existing FVG check must be anchored to the confirmation
  bar (`sw2_conf_idx + 1`), not the current scanning bar (`i`), so
  `entry_price` and FVG membership always reference the same bar.
- `smt_time` must be the confirmation-bar timestamp (`sw2_conf_idx + 1`),
  not the outer-loop scan bar `i`. Cached candidates can be re-emitted
  many bars later; stamping `row['et']` would attach a later, unrelated bar.
- Confirmation (`sw2_conf_idx + 1`) must be within `MAX_CONF_GAP_MINS` of
  `sw2_conf_time`. `sdf` is session + pre_session only, so index+1 can
  jump across a session gap (14:29 → next 07:30). Those signals must be
  dropped, not priced on the distant bar. Piece 3 must fail any output
  row that violates this (`check_sw2_confirmation_gap`).
- Signal dedup must be based on `sw2_conf_time` within a 30-min session bucket,
  per direction per instrument, to prevent duplicate signals.
- SMT divergence must be measured on **wicks**, at structural
  swing-marker-tagged points only — never candle bodies or non-structural
  points. (Audit-confirmed as the primary driver of false signals and losses.)
- Equal-high/equal-low SMT requires exact price match (`EQUAL_THRESHOLD = 0.0`)
  — never a tolerance band.
